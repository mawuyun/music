export default class song {
  constructor({blbum, id, image, singer, name, url, mid, duration}) {
    this.singer = singer;
    this.blbum = blbum;
    this.image = image;
    this.name = name;
    this.url = url;
    this.mid = mid;
    this.duration = duration;
    this.id = id;
  }
}
